import { DeviceDirective } from './device.directive';

describe('DeviceDirective', () => {
  it('should create an instance', () => {
    const directive = new DeviceDirective();
    expect(directive).toBeTruthy();
  });
});
